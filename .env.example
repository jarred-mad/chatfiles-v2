# ===========================================
# ChatFiles.org Environment Configuration
# ===========================================

# ----- Meilisearch Configuration -----
# URL of your Meilisearch instance
MEILISEARCH_URL=http://localhost:7700
# Master key for Meilisearch (generate a secure key for production)
MEILISEARCH_API_KEY=your_master_key_here

# ----- PostgreSQL Database -----
# Full connection string for PostgreSQL with pgvector extension
DATABASE_URL=postgresql://chatfiles:password@localhost:5432/chatfiles
# For Docker Compose, the password is used separately
POSTGRES_PASSWORD=your_secure_password

# ----- Cloudflare R2 Storage -----
# Your Cloudflare account ID
R2_ACCOUNT_ID=your_account_id
# R2 API credentials (create in Cloudflare dashboard)
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
# Bucket name for storing files
R2_BUCKET_NAME=chatfiles-archive
# Public URL for accessing files (use custom domain or R2 public URL)
NEXT_PUBLIC_R2_PUBLIC_URL=https://files.chatfiles.org

# ----- Google AdSense -----
# Your AdSense publisher ID (ca-pub-XXXXXXXX)
NEXT_PUBLIC_ADSENSE_CLIENT_ID=ca-pub-XXXXXXXXXX

# ----- Optional: Analytics -----
# Google Analytics measurement ID
NEXT_PUBLIC_GA_ID=

# ----- Optional: Stripe (for premium tier) -----
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
